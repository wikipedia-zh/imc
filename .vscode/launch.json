{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Jekyll",
            "request": "launch",
            "type": "pwa-msedge",
            // "preLaunchTask": "jekyll",
            "url": "http://127.0.0.1:4000",
            "webRoot": "${workspaceFolder}/_site",
        }
    ]
}
